# 拾味庭 Vue 3 首页

这是一个使用 Vue 3 和 Vite 构建的项目，不能通过双击 `index.html` 运行。

## 启动方式

双击 `start.command`，或在项目目录运行：

```bash
npm run start
```

然后访问：

```text
http://127.0.0.1:4173/
```

## 其他命令

```bash
npm run build
npm run preview
```
